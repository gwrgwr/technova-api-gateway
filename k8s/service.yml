apiVersion: v1
kind: Service
metadata:
  name: technova-api-gateway-service
  namespace: technova
spec:
  selector:
    app: technova-api-gateway
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort